package game.gameObjects;


/**
 * This gamelement is a fillelement which have the pourpose to fill a empty place in the map so that
 * the element on this field is not null.
 */
public class EmptySpace extends SpielElement {

    public EmptySpace(int xPos, int yPos) {
        super(xPos, yPos);
    }
}
